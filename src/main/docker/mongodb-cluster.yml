version: '2'
services:
  wisestepapp-mongodb:
    image: mongo:4.0.10
    ports:
      - '27017:27017'
    command: mongos --configdb wisestepapp-mongodb-config
  wisestepapp-mongodb-node:
    build:
      context: .
      dockerfile: mongodb/MongoDB.Dockerfile
    command: mongod --replSet rs1 --noprealloc --smallfiles
  wisestepapp-mongodb-config:
    image: mongo:4.0.10
    container_name: wisestepapp-mongodb-config
    command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
